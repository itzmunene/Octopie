version: "3.9"

services:
  digital-immune-system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: Octopie
    image: octopie:0.0.1
    volumes:
      - .:/workspace
    working_dir: /workspace
    environment:
      - PYTHONUNBUFFERED=1
    command: bash
    tty: true
